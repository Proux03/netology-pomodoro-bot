meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 17
build:
  artifacts:
    build/libs/*.jar: /
run:
  jarName: netology-pomodoro-bot-1.0-SNAPSHOT-all.jar
  persistenceMount: /data
  containerPort: 80
